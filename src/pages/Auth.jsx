/**
 * Render the Auth page heading.
 * @returns {JSX.Element} A React element containing an h1 with the text "Auth page".
 */
export default function Auth(){
    return(
        <>
            <h1>Auth page</h1>
        </>
    )
}